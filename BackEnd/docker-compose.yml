services:
  db:
    image: mysql:8.0
    container_name: stock
    ports:
      - "1202:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: stock
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: